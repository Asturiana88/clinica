import { FormValidationsDirective } from './form-validations.directive';

describe('FormValidationsDirective', () => {
  it('should create an instance', () => {
    const directive = new FormValidationsDirective();
    expect(directive).toBeTruthy();
  });
});
